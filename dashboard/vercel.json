{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "${RAILWAY_API_URL}/api/$1" },
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}

